server:
  port: 8080



spring:
  application:
    name: Komas amigoscode sample springboot app
  datasource:
    url: jdbc:postgresql://localhost:5332/customer
    username: mesmeradm
    password: admin
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  main:
    web-application-type: SERVLET


